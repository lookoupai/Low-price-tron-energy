# 定时推送卡住问题修复记录

## 问题描述

**时间：** 2026-09-03  
**症状：** 频道从中午查询后一直没有新的查询信息发布

## 问题分析

### 日志显示
```
2026-09-03 13:59:46 - 开始广播地址信息
2026-09-03 14:59:35 - WARNING - Execution of job "broadcast_addresses" skipped: maximum number of running instances reached (1)
2026-09-03 15:59:35 - WARNING - Execution of job "broadcast_addresses" skipped: maximum number of running instances reached (1)
... (持续到 19:59)
```

### 根本原因

1. **13:59 的任务卡住未完成**
   - `broadcast_addresses` 函数在调用 TronScan API 时遇到网络超时或响应慢
   - 没有设置超时控制，任务一直等待，无法结束

2. **APScheduler 任务实例限制**
   - 默认设置 `max_instances=1`，同一任务只能有一个实例运行
   - 上一个实例未结束，后续所有小时的定时任务都被跳过

3. **缺少超时保护**
   - API 请求没有超时设置
   - 整个广播任务没有超时保护
   - 导致一旦卡住就无法自动恢复

## 解决方案

### 1. 为广播任务添加整体超时控制

**文件：** `telegram_bot.py:1336`

```python
async def broadcast_addresses(self, context: ContextTypes.DEFAULT_TYPE, specific_chat_id: Optional[int] = None) -> None:
    """向活跃的频道广播地址信息，带整体超时控制"""
    try:
        async def _do_broadcast():
            # 原有的广播逻辑
            ...
        
        # 设置 5 分钟超时，防止任务无限期卡住
        await asyncio.wait_for(_do_broadcast(), timeout=300)
        logger.info("广播地址任务完成")
        
    except asyncio.TimeoutError:
        logger.error(f"广播地址任务超时（5分钟），已强制结束")
    except Exception as e:
        logger.error(f"广播地址时出错: {e}")
```

**效果：**
- 每次广播任务最多运行 5 分钟
- 超时后自动结束，释放任务锁
- 下一个小时的定时任务可以正常启动

### 2. 为 API 请求添加超时控制

**文件：** `tron_energy_finder.py:494`

```python
async def _make_request(self, url: str, params: Dict = None, timeout: int = 30) -> Optional[Dict]:
    """发送 API 请求，带超时控制"""
    try:
        api_key = await self.api_manager.get_next_key()
        headers = {
            "TRON-PRO-API-KEY": api_key,
            "Accept": "application/json"
        }
        
        connector = aiohttp.TCPConnector(ssl=self._ssl_context)
        timeout_obj = aiohttp.ClientTimeout(total=timeout)
        
        async with aiohttp.ClientSession(connector=connector, timeout=timeout_obj) as session:
            async with session.get(url, params=params, headers=headers) as response:
                if response.status == 200:
                    return await response.json()
                else:
                    logger.error(f"API请求失败: {response.status}")
                    return None
                    
    except asyncio.TimeoutError:
        logger.warning(f"API请求超时 ({timeout}s): {url}")
        return None
    except Exception as e:
        logger.error(f"请求失败: {e}")
        return None
```

**效果：**
- 每个 API 请求最多等待 30 秒
- 超时后返回 None，不会阻塞后续逻辑
- 防止单个慢速 API 拖垮整个查询流程

### 3. 为定时任务添加名称标识

**文件：** `telegram_bot.py:1476`

```python
job_queue.run_repeating(
    self.broadcast_addresses,
    interval=3600,
    first=300,
    name="broadcast_addresses"  # 添加任务名称
)
```

**效果：**
- 便于在日志中追踪任务状态
- 方便后续调试和监控

## 超时层级设计

```
总超时控制（5分钟）
  ├─ API 请求超时（30秒/次）
  ├─ 并发信号量控制（最多3个并发）
  └─ 多次 API 调用累积
```

**说明：**
- 单次查询涉及多个 API 调用（获取区块、获取交易、分析地址等）
- 每个 API 最多 30 秒，整体不超过 5 分钟
- 即使遇到慢速 API，也能在 5 分钟内强制结束

## 测试验证

### 重启验证
```bash
docker compose restart bot
```

### 预期行为
1. 容器重启后，机器人正常启动
2. 启动后 5 分钟（20:21），首次定时任务触发
3. 如果 API 超时，30 秒后自动跳过该请求
4. 如果整体任务超时，5 分钟后强制结束
5. 下一小时（21:21）定时任务能正常启动

### 日志监控
```bash
# 查看定时任务执行情况
docker compose logs -f bot | grep -E "(broadcast|广播|超时|完成)"

# 查看 API 超时情况
docker compose logs -f bot | grep -E "(超时|timeout|TimeoutError)"
```

## 后续建议

### 1. 监控告警
- 如果频繁出现超时，考虑增加 API Key 数量
- 如果总是在某个时间点超时，可能是 TronScan API 服务端问题

### 2. 性能优化
- 考虑增加结果缓存时间，减少 API 调用频率
- 优化并发查询策略，提高查询效率

### 3. 容错机制
- 当前修复已添加超时保护
- 如需更高可用性，可考虑添加 API 故障重试机制

## 修复时间

- **发现时间：** 2026-09-03 20:16
- **修复时间：** 2026-09-03 20:17
- **重启时间：** 2026-09-03 20:16
- **预计恢复：** 2026-09-03 20:21（启动后5分钟首次任务）
